appId: com.trusttools.desktop
productName: TrustTools
asar: true
npmRebuild: false
directories:
  output: release
files:
  - build/electron/**/*
  - package.json
  - "!node_modules/**/*"
extraResources:
  - from: .output
    to: web
    filter:
      - "**/*"
  - from: build/tray-icon.png
    to: tray-icon.png
  - from: vendor/tokentracker-cli
    to: tokentracker-cli
    filter:
      - "bin/**/*"
      - "src/**/*"
      - "LICENSE"
      - "package.json"
      - "package-lock.json"
  - from: vendor/tokentracker-cli/node_modules
    to: tokentracker-cli/node_modules
    filter:
      - "**/*"
afterPack: electron/after-pack.cjs
mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
  icon: build/icon.png
  category: public.app-category.developer-tools
  hardenedRuntime: true
  gatekeeperAssess: false
dmg:
  artifactName: "${productName}-${version}-${arch}.${ext}"
win:
  target:
    - target: nsis
      arch:
        - x64
  icon: build/icon.png
nsis:
  artifactName: "${productName}-Setup-${version}-${arch}.${ext}"
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
