/**
 * The AITracker product version. Kept as a single literal so the UI
 * (AppShell brand, About page) and the version-check server fn share one
 * source. Bump this together with package.json `version` on release.
 */
export const APP_VERSION = "3.0.1";

/** Release date shown on the About page. */
export const APP_RELEASE_DATE = "2026-08-03";

/** Source repository link shown on the About page. */
export const APP_REPO_URL = "https://github.com/trusttools/trusttools";
