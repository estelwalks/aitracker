# AITracker Clean Room 开发记录

## 目标

AITracker 采用独立实现。外部项目只用于确认可观察行为、数据位置和公开文件格式，不复制源码、组件结构、命名、注释、资源或测试。

## 参考登记

| 项目         | 本地快照                                  | 许可 | 允许参考                                 | 禁止复用                                       |
| ------------ | ----------------------------------------- | ---- | ---------------------------------------- | ---------------------------------------------- |
| AITracker | `/Users/lijy/RedactedWorkspace/AITracker` | MIT  | 本地日志发现、增量读取和聚合这一行为思路 | 源码、函数结构、类型、UI、文案、资源、接口设计 |

## 独立实现原则

1. 先从 AITracker PRD 定义输入、输出和验收标准。
2. 数据采集器按各工具公开或本机可观察的日志格式独立编写。
3. 所有模块使用 AITracker 自有类型、目录划分、缓存格式和错误模型。
4. 不复制参考项目的代码片段后再改名或改写。
5. 任何新参考资料都必须先登记用途、许可和边界。

## 发布门禁

- [x] 仓库包含 MIT `LICENSE`。
- [x] 仓库包含 `NOTICE`。
- [x] 参考项目及允许参考范围已登记。
- [ ] 依赖许可证清单已生成并复核。
- [ ] 相似度扫描已执行并归档。
- [ ] macOS 与 Windows 安装包第三方声明已核对。
- [ ] 发布负责人完成最终签核。

## 隐私口径

AITracker 的承诺是：不采集、不持久化、不上传对话正文。日志解析可能在本机内存中读取包含 usage 字段的完整记录，但只提取 Token、模型、来源、项目、会话标识和时间等统计字段。
