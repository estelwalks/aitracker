# Skill 管理 · 模块核对报告

> 依据：原型图（`project-sparkle-hub-92-main-1`，Lovable 导出 React 项目）+ PRD v1.2（`TrustTools-V3.0-产品需求文档.md`，2026-08-03）
> 日期：2026-08-05 · 分支：feature/init
> 方法：按原型 → PRD → 当前实现三方逐项核对；差异项由产品决策后落地。

## 1. 核对矩阵（EPIC-02：FR-014~017）

| #   | 需求点                             | 原型                                                                     | PRD v1.2                                                                                                                               | 当前实现                                                   | 结论                                                        |
| --- | ---------------------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ----------------------------------------------------------- |
| 1   | 页头与副标题                       | 「Skill 管理」+「本地共 84 个 Skill · 安装位置 · 同步 · 卸载」+ 刷新扫描 | FR-014：标题 + **副标题含本地 Skill 总数**                                                                                             | ⚠️ 副标题为「扫描于 {time}」，总数在表格标题               | **已补齐**：副标题改为「共 N 个本地 Skill · 扫描于 {time}」 |
| 2   | 搜索/安装位置筛选/名称排序/分页 25 | ✅                                                                       | FR-014：模糊搜索回第一页、安装位置下拉、名称升降序、分页栏含范围/首页/末页禁用                                                         | ✅ 全部实现                                                | 一致                                                        |
| 3   | 表格列                             | 复选框/名称+来源/描述/安装位置/调用/日均/最近使用/操作                   | FR-014：复选框/名称(含来源路径)/描述/安装位置/最近使用时间/操作（§7.3 数据边界：**不采集 Skill 调用次数及调用行为**）                  | ⚠️ 曾含 调用/日均/趋势 三列 + 健康圆点                     | **已移除三列**（见 D2），现为 FR-014 六列                   |
| 4   | 详情查看                           | 右侧 Drawer：名称/来源/描述/安装状态/趋势                                | FR-015：名称/来源路径/描述/安装状态                                                                                                    | ✅ Drawer（版本/更新状态/逐 agent 安装卸载/黑名单为超集）  | 一致                                                        |
| 5   | Skill 删除（单条+批量+二次确认）   | ✅                                                                       | FR-016：警告图标/确认卸载/从全部已安装工具移除/不可撤销/移除后清勾选提示                                                               | ✅ AlertDialog                                             | 一致                                                        |
| 6   | 跨 Agent 同步                      | ✅                                                                       | FR-017 SHOULD：全局/指定模式、未安装 agent 标「未安装」不可勾选、**冲突确认弹窗（逐条覆盖/跳过 + 全部覆盖/跳过）**、结果汇总 成功/跳过 | ✅ 全部实现（含冲突弹窗）                                  | 一致                                                        |
| 7   | 空状态                             | 「没有匹配的 Skill」+ 添加监控目录/去市场                                | ✅                                                                                                                                     | ✅                                                         | 一致                                                        |
| 8   | **健康度体系**                     | 数据层定义 4 态（active/low/doze/dead）但 **UI 未渲染**                  | **v1.2 已明确「删除 Skill 健康度评估体系，改为 Skill 数量统计」**（需求简报 v2.2/v2.3 记录；v1.2 全文无"健康度"字样）                  | ⚠️ 曾深度实现 5 态 + 筛选 + 行内圆点 + 阈值可配 + 首页分布 | **已完全移除**（见 D1）                                     |
| 9   | Agent 范围                         | 32 个（mock）                                                            | 27 工具（FR-014）；实施决策：skill 安装目标为**有 skill 目录的 5 个**（Claude Code/Codex CLI/Cursor/Gemini CLI/OpenCode）              | ✅ 5 个（`SKILL_TOOL_NAMES`）                              | 一致（既定决策）                                            |
| 10  | 数据刷新                           | 刷新扫描按钮                                                             | FR-014 手动重扫                                                                                                                        | ✅ 按钮 + 5 秒指纹轮询（可见时）                           | 一致                                                        |
| 11  | 黑名单/回收站                      | 原型有「移至回收站」                                                     | PRD 无                                                                                                                                 | 黑名单（block/unblock）为超集                              | 保留（超集）                                                |

## 2. 决策记录

### D1：完全移除健康度评估体系（2026-08-05，产品确认）

- **依据**：PRD v1.2 已删除健康度概念（需求简报 v2.2「删除 Skill 健康度评估体系，改为 Skill 数量统计」）；原型 UI 亦未展示（mock 数据层残留 4 态定义未渲染）。
- **动作**：删除 `SkillHealth` 类型、scanner 的 `healthFromLastUse`/阈值配置/`skillDailySeries`、`SkillSnapshot.healthBasis`、skills 页健康度筛选下拉与行内圆点、首页 Skill 卡分布圆点、`dashboard.model` 之外的全部 `skills.health.*` i18n 键；相关 2 个单测用例改写。
- **差异标注**：PRD 曾于旧版 v3.2 定义「Skill 健康度分析（7 天≥5 次/日均频率/趋势四维）」（见 PRD实现审计报告-2026-07-31），v1.2 已废除；本次实现同步移除，前后一致。

### D2：移除「调用 / 日均 / 趋势」三列（2026-08-05，产品确认）

- **依据**：PRD §7.3 数据边界明确「不采集 Skill 调用次数及调用行为」（隐私最小化）；FR-014 表格列又保留「最近使用时间」——文档自相矛盾，以 §7.3 数据边界为准。
- **动作**：表格收敛为六列（复选框/名称含来源路径/描述/安装位置/最近使用时间/操作）；scanner 不再统计 usageCount/按日序列/趋势，仅保留 `lastUsedAt`（最近使用时间列的来源，来自日志脱敏的 `context.skills` 时间戳）。
- **保留**：最近使用时间列（原型与 FR-014 均有；无调用记录时显示「暂无可验证记录」）。

### D3：副标题补齐本地 Skill 总数（2026-08-05）

- FR-014 要求副标题含总数；改为「共 {count} 个本地 Skill · 扫描于 {time}」，与首页 Skill 数卡片联动。

## 3. 待办（后续迭代）

- 设置页健康阈值配置项（若存在残留字段）确认清理；`trusttools-prefs.json` 中 `lowFrequencyCount/dozeDays/deadDays` 旧键不再读取
- 详情抽屉「版本/更新状态」等超集字段保持现状（PRD 未要求，未做改动）
- 实施进度文档 B3~B6/E 行等状态行与代码进度存在滞后，建议下次文档同步时全量刷新

## 4. 识别逻辑校验（2026-08-05）

依据本地样本和产品规则校验 skill 发现逻辑，修复三个缺陷：无 marker 容器目录被整体识别（`development/` → 单个 skill）、根级 README.md 被误识别、嵌套 skill 目录无法枚举。

| 维度       | 对齐前                                                | 对齐后                                                                                                          |
| ---------- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| skill 判定 | 任意 `.md` 文件算 skill；目录无 SKILL.md 兜底算 skill | **目录内含 marker（`SKILL.md`/`skill.md` 白名单）才算**；裸 .md（README 等）一律不算                            |
| 嵌套目录   | 只扫一层，`development/xxx/SKILL.md` 丢失             | **递归 maxDepth=3**：命中 marker 即记且不再下钻，无 marker 且有余量才下钻                                       |
| 命名       | 目录名 / 文件名去 .md                                 | **frontmatter `name` > 父目录名 > "Skill"**（本机 SKILL.md 的 name 与目录名一致，显示不变）                     |
| 规则位置   | catalog.ts 硬编码 `skillRootSuffix`（5 个）           | **独立配置文件 `src/lib/local-skills/agent-rules.ts`**，每工具一组规则（toolId/roots/envHome/markers/maxDepth） |
| 工具范围   | 5 个                                                  | **9 个**：+grok（GROK_HOME）、hermes、openclaw、antigravity（双根）；claude 仅扫 `~/.claude/skills`             |
| 路径守卫   | 仅接受根下直接子级                                    | 任意深度 + 无 `..` 段 + realpath 防符号链接逃逸（嵌套安装/卸载/同步可用）                                       |

行为差异记录：`~/.claude/dev` 不扫描，仅 `~/.claude/skills`；zcode 仅插件缓存不扫目录、agents 不在本产品 27 工具 catalog 内，均不进配置。
