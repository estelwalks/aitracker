---
name: configuration-template
description: Generate an example configuration containing obvious placeholders.
---

# Configuration template

Copy `config.example.toml` and replace every documented placeholder through your
approved secret manager. The example values are deliberately non-secret.
