---
name: service-health-check
description: Check an explicitly configured HTTPS health endpoint.
---

# Service health check

The helper performs a read-only request to the documentation example domain. It
uses TLS, accepts no credentials, and does not upload local data.
