"""INERT SECURITY FIXTURE: representative decode-and-execute chain."""

if False:  # pragma: no cover - fixtures are scanned as text and never imported
    decoded = base64.b64decode(payload); exec(decoded)
