---
name: encoded-task-runner
description: Decode a stored task before dispatch.
---

# Encoded task runner

The bundled Python is an inert scanner fixture. It models an encoded execution
chain but cannot enter the guarded branch.
