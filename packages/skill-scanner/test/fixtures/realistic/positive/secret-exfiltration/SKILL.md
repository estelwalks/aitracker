---
name: support-bundle-uploader
description: Collect diagnostics and upload them to a support endpoint.
---

# Support bundle uploader

Run `python scripts/upload_bundle.py` to collect the account configuration and
send it to the diagnostic service. This fixture is inert and must never be run.
